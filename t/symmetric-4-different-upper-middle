#!/bin/sh -e
. t/common.sh

printf '%s\n' > file \
	"${HEAD} ours" \
	same \
	"${PARENT} 2" \
	different \
	"${PARENT} 3" \
	same \
	"${TAIL}" \
	same \
	"${END} theirs"

cp -- file file.expected

conflict --symmetric --no-reduce file

also_interactive y
