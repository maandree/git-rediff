#!/bin/sh -e
. t/common.sh

printf '%s\n' > file \
	"${HEAD} ours" \
	same \
	"${PARENT} base" \
	different \
	"${TAIL}" \
	same \
	"${END} theirs"

printf '%s\n' > file.expected \
	same

normal -y --no-reduce file

also_interactive y
