#!/bin/sh -e
. t/common.sh

printf '%s\n' > file \
	"${HEAD} ours" \
	new \
	"${PARENT} base" \
	old \
	"${TAIL}" \
	new \
	"${END} theirs"

printf '%s\n' > file.expected \
	new

normal --no-symmetric --symmetric --no-reduce file

also_interactive y
