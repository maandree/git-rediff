#!/bin/sh -e
. t/common.sh

printf '%s\n' > file \
	"${HEAD} ours" \
	same \
	"${PARENT} base" \
	same \
	"${TAIL}" \
	different \
	"${END} theirs"

printf '%s\n' > file.expected \
	different

normal -m --no-reduce file

also_interactive s
