#!/bin/sh -e
. t/common.sh

printf '%s\n' > file \
	"${HEAD} ours" \
	common \
	"${PARENT} base" \
	common \
	"${TAIL}" \
	"${END} theirs"

cp -- file file.expected

conflict --no-reduce file

also_interactive s
